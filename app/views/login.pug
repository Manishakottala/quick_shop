doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    title QuickShop | Login
    link(rel="stylesheet" href="/style.css")
  body
    include navbar.pug

    section.auth-page
      .auth-card
        h2 Welcome Back
        p.auth-text Sign in with your QuickShop account to continue browsing products.

        if authError
          p.auth-message.auth-message-error= authError

        if authSuccess
          p.auth-message.auth-message-success= authSuccess

        form.auth-form(action="/login" method="post")
          .form-group
            label(for="email") Email Address
            input#email(type="email" name="email" placeholder="you@example.com" required)

          .form-group
            label(for="password") Password
            input#password(type="password" name="password" placeholder="Enter your password" required)

          button(type="submit") Login

        p.auth-switch
          | New to QuickShop?
          a(href="/register") Create an account

    footer
      p © 2026 QuickShop. All rights reserved.
